# Local dev: point at your local API (no trailing slash).
# ESP32 devices POST to: {NEXT_PUBLIC_API_URL}/data
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production API on Render (also use this value in Vercel → Environment Variables):
NEXT_PUBLIC_API_URL=https://backend-benthic-guardian.onrender.com
